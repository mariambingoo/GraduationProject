const fs = require('fs');
const Model = require('../models/ModelModel.js');

const addModel = async (req, res) => {
    
}
module.exports = {
  getTrainingData,
  getModelWeights,
  getPlots,
  saveTrainData,
  getTrainData
};
